name: build-packages-release-nikki-tproxy

on:
  workflow_dispatch:
  
permissions:
  contents: write
  
jobs:
  build:
    name: ${{ matrix.arch }}-${{ matrix.branch }} build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch:
          - aarch64_generic
        branch:
          - openwrt-24.10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build package
        uses: openwrt/gh-action-sdk@main
        env:
          ARCH: ${{ matrix.arch }}-${{ matrix.branch }}
          FEEDNAME: nikki
          PACKAGES: luci-app-nikki
          NO_REFRESH_CHECK: true

      - name: Extract package version
        id: extract_version
        run: |
          set -e
          PKG_FILE=$(find bin/packages/${{ matrix.arch }}/nikki -name 'nikki_*.ipk' | head -n 1)
          if [[ -z "$PKG_FILE" ]]; then
            echo "❌ Package file not found!"
            exit 1
          fi
          VERSION=$(basename "$PKG_FILE" | sed -E 's/^nikki_([^_]+)_.*\.ipk/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create release filename and tag
        id: release_meta
        run: |
          TODAY=$(date +'%Y-%m-%d')
          # Format nama file
          #REL_NAME="Nikki-TProxy-${TODAY}-${{ steps.extract_version.outputs.version }}_${{ matrix.arch }}-${{ matrix.branch }}"
          REL_NAME="Nikki-TProxy-${{ steps.extract_version.outputs.version }}_${{ matrix.arch }}-${{ matrix.branch }}"
          # Format tag name aman (hilangkan karakter ilegal jika ada)
          SAFE_TAG=$(echo "$REL_NAME" | tr '~ ' '--')
          echo "relname=$REL_NAME" >> $GITHUB_OUTPUT
          echo "tagname=$SAFE_TAG" >> $GITHUB_OUTPUT

      - name: Compress .ipk files
        run: |
          mkdir -p output
          cp bin/packages/${{ matrix.arch }}/nikki/*.ipk output/
          zip -j ${{ steps.release_meta.outputs.relname }}.zip output/*.ipk

      - name: Create empty commit and tag
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git fetch origin
          git checkout --orphan temp-release-branch
          git rm -rf .
          git commit --allow-empty -m "Empty commit for release"
          git tag Nikki-TProxy-${{ steps.extract_version.outputs.version }}_${{ matrix.arch }}-${{ matrix.branch }}-${{ github.run_number }}
          git push origin Nikki-TProxy-${{ steps.extract_version.outputs.version }}_${{ matrix.arch }}-${{ matrix.branch }}-${{ github.run_number }}

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
        with:
          tag_name: ${{ steps.release_meta.outputs.tagname }}
          release_name: ${{ steps.release_meta.outputs.relname }}
          draft: false
          prerelease: false

      - name: Upload ZIP to GitHub Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ${{ steps.release_meta.outputs.relname }}.zip
          asset_name: ${{ steps.release_meta.outputs.relname }}.zip
          asset_content_type: application/zip
